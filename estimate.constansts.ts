export const DEFAULT_DAYS_COUNT = 30;
export const DEFAULT_MONTHS_COUNT = 6;
export const MONTH_FORMAT = 'MMM';

export const DATA_SOURCES = {
  E_DATA: 'e_data',
  OWN_DATA: 'own_data',
  USE_BOTH: 'use_both',
};

export const DEFAULT_DATA_SOURCE = DATA_SOURCES.USE_BOTH;
